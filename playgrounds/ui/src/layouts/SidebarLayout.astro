---
import { Sidebar } from '@studiocms/ui/components';
import { RootLayout, type RootLayoutProps } from '@studiocms/ui/layouts';

type Props = RootLayoutProps;

const { title, description, image, headers, lang = 'en', defaultTheme = 'dark' } = Astro.props;
---
<RootLayout 
  {title}
  {description}
  {image}
  {headers}
  {lang}
  {defaultTheme}
>
  <main class="main">
    <Sidebar>
      <a href="/">Components</a>
      <a href="/double-sidebar">Double Sidebar</a>
    </Sidebar>
    <div class="content">
      <slot />
    </div>
  </main>
</RootLayout>
<style>
  .main {
    width: 100vw;
    height: 100vh;
    overflow: hidden;
    display: block;
    position: relative;
  }

  .content {
    width: 100%;
    height: 100%;
    display: flex;
    align-items: center;
    justify-content: center;
    text-align: center;
  }
</style>