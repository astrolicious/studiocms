import {
	toCamelCase as tCC,
	toPascalCase as tPC,
} from '@matthiesenxyz/integration-utils/textFormatters';

const toCamelCase = tCC;
const toPascalCase = tPC;

export { toCamelCase, toPascalCase };
