---
type Props = {
	alignCenter?: boolean;
	gapSize?: 'sm' | 'md' | 'lg';
};

const { alignCenter, gapSize } = Astro.props;
---

<div class="row" class:list={[alignCenter && "align", gapSize]}>
  <slot />
</div>
<style>
  .row {
    display: flex;
    flex-direction: row;
    position: relative;
  }

  .row.align {
    align-items: center end;
  }

  .row.sm {
    gap: .5rem;
  }

  .row.md {
    gap: 1rem;
  }

  .row.lg {
    gap: 2rem;
  }
</style>