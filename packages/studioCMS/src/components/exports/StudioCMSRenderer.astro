---
import Config from 'virtual:studiocms/config';
import { contentRenderer } from '../../utils/renderers/contentRenderer';

type Props = {
	content: string;
};

const { content } = Astro.props;
---

<Fragment set:html={contentRenderer(content, Config.contentRenderer)} />