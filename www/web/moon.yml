tags: ['astro']

toolchain:
  typescript:
    syncProjectReferences: false

tasks:
  astro:
    command: astro
    platform: node
  dev:
    command: 'astro dev'
    local: true
    platform: node
  start:
    command: 'astro dev'
    local: true
    platform: node
  preview:
    command: 'astro preview'
    local: true
    platform: node
  build:
    command: 'astro check && astro build'
    local: true
    platform: node
