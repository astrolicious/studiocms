---
import { Code } from "astro/components";
---
<h1>Setup Done!</h1>

<p>You can now stop your dev server and add the following option in your astro.config to start using Astro-Studio-CMS then start back up to see your new CMS data</p>

<Code lang="diff" code={`import { defineConfig } from "astro/config";
import studioCMS from "@nametbd/astro-studio-cms";
import db from '@astrojs/db';

// https://astro.build/config
export default defineConfig({
    // other config options,
	integrations: [
		db(),
		studioCMS({ 
+			dbStartPage: false,
		}),
	],
});`}/>